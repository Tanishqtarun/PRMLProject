from sklearn.datasets import fetch_openml 
from sklearn.model selection import train_test_split

mnist = fetch_openml('mnist_784', version=1)
X, y = mnist.data,
mnist.target.astype(int) # Convert labels to integers
